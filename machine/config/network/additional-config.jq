# additional network configuration

# this file is individually for the project and will not be updated by copier
# add the configuration into the function below, the returned object will be merged with the `machine.network` object

def additional_config:
  # must return a json object
  {
    # "an_additional_setting": true
  };
