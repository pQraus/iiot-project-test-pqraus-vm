# additional cluster configuration

# this file is individually for the project and will not be updated by copier
# add the configuration into the function below, the returned object will be merged with the `machine.cluster` object

def additional_config:
  # must return a json object
  {
    # "an_additional_cluster_setting": true
  };
